name: Deploy Dev Hello World Template

on:
  push:
    branches: [dev]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-dev-${{ github.ref }}
  cancel-in-progress: true

jobs:
  preflight:
    runs-on: ubuntu-latest
    outputs:
      enabled: ${{ steps.check.outputs.enabled }}

    steps:
      - name: Check whether Azure deployment is enabled
        id: check
        run: |
          if [ "${{ vars.ENABLE_AZURE_DEPLOY }}" = "true" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
            echo "::notice::Azure deployment is enabled for this repository."
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Azure deployment is disabled for this template repository. Configure Azure + GitHub environments, then set the repository variable ENABLE_AZURE_DEPLOY=true to turn on dev deployments."
          fi

  deploy:
    needs: preflight
    if: ${{ needs.preflight.outputs.enabled == 'true' }}
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: function-app/package-lock.json

      - name: Install + validate function
        working-directory: function-app
        run: |
          npm ci
          npm run typecheck
          npm test
          # Keep deployment package small and production-focused.
          npm prune --omit=dev

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy infrastructure (Bicep)
        id: infra
        env:
          AZURE_CORE_OUTPUT: none
        run: |
          # Deploy infra only after the app has passed validation checks.
          FUNCTION_APP_NAME=$(az deployment group create \
            -g "${{ vars.AZURE_RG }}" \
            -f infra/main.bicep \
            -p @infra/main.parameters.dev.json \
            --query "properties.outputs.functionAppResourceName.value" \
            -o tsv)

          if [ -z "$FUNCTION_APP_NAME" ]; then
            echo "Bicep output functionAppResourceName is empty." >&2
            exit 1
          fi

          echo "FUNCTION_APP_NAME=$FUNCTION_APP_NAME" >> "$GITHUB_ENV"

          FUNCTION_HOST_NAME=$(az functionapp show \
            -g "${{ vars.AZURE_RG }}" \
            -n "$FUNCTION_APP_NAME" \
            --query defaultHostName \
            -o tsv)

          if [ -z "$FUNCTION_HOST_NAME" ]; then
            echo "Function host name is empty." >&2
            exit 1
          fi

          echo "FUNCTION_HOST_NAME=$FUNCTION_HOST_NAME" >> "$GITHUB_ENV"

      # Create a deterministic deployable artifact
      - name: Create deployment package (zip)
        run: |
          cd function-app
          zip -r ../functionapp.zip host.json package.json package-lock.json dist node_modules \
            -x "**/.git/**" \
            -x "**/.github/**" \
            -x "**/.vscode/**" \
            -x "**/local.settings.json" \
            -x "**/node_modules/.cache/**"

      - name: Deploy Function App code
        uses: Azure/functions-action@v1
        with:
          app-name: ${{ env.FUNCTION_APP_NAME }}
          package: functionapp.zip

      - name: Smoke test deployed app
        run: |
          for attempt in 1 2 3 4 5 6 7 8; do
            RESPONSE=$(curl -fsS --max-time 20 "https://${FUNCTION_HOST_NAME}/" || true)

            if [ "$RESPONSE" = "Hello World" ]; then
              echo "Smoke test passed."
              exit 0
            fi

            echo "Smoke test attempt ${attempt} did not return the expected response yet."
            sleep 15
          done

          echo "Smoke test failed. Expected 'Hello World' from https://${FUNCTION_HOST_NAME}/" >&2
          exit 1
